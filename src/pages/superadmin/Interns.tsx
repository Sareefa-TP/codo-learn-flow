import InternList from "@/components/intern/InternList";

/**
 * Super Admin Intern Management
 * This page uses the shared InternList component.
 */
const Interns = () => {
  return <InternList />;
};

export default Interns;
