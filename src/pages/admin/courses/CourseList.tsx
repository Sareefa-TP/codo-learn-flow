import CourseList from "@/components/course/CourseList";

/**
 * Admin Course Management
 * This page uses the shared CourseList component.
 */
const AdminCourses = () => {
  return <CourseList />;
};

export default AdminCourses;
