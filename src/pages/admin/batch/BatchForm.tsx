import BatchForm from "@/components/batch/BatchForm";

/**
 * Admin Batch Form (Create/Edit)
 * This page uses the shared BatchForm component.
 */
const AdminBatchFormPage = (props: any) => {
  return <BatchForm {...props} />;
};

export default AdminBatchFormPage;
